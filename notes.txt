THIS IS GOING TO REQUIRE NL PARSING

Handle X... tricky because it's woven throughout at least 2 different grammar pieces...

detect "X, Y, or Z": Use spacy dep parse: conj!

reg = r"([A-Z]{2,})(, \1)*,? (and|or) \1"
matches
- MANA, MANA, or MANA
- COLOR and COLOR
- COLOR or COLOR
- etc
DOESNT match \+1
- g or g
- E or E

Going forward: this probably needs to be a special step DURING parsing.
 - Importantly, there may be NON-ATOM RULES that this should apply to...
		that may make it tricky to know where to add this
		(likely related to rule ordering)
 - I think the best approach is to collapse TAG, TAG, or TAG into just TAG...

Similar but different syntax : CTYPE CTYPE -> CTYPE

Need to remove ability words "whatever — Do the thing", BUT
	some " — " aren't ability words: max speed (???), sagas
	the " — " rabbit hole is VERY DEEP @_@

Add "choose N" bullets to effects (could also do {P} above, actually...)

Wow... this does NOT work: 
doc = nlp(c['text'])
for token in doc:
    if "PROPN" in token.pos_:
		...
e.g. doc = nlp('Defender\nFlying') marks "Defender" as PROPN... \facepalm

Any downside to lemmatizing everything as first step of atomize??
 - YES: "enchant" vs "enchanted"

NEXT BIG STEP: add current parser post-processing: see what still needs to be parsed AND
automatically analyze un-parsed text (look in "subs", find patterns, etc)
 - do it automatically/deeply for any patterns with *
	
Another thing: get list of all keyword abilities (@_@) and make grammar rules for them